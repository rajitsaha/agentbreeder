name: ollama-local-agent
version: 0.1.0
description: "Local agent powered by Ollama — no API keys required"
team: engineering
owner: dev@example.com
tags:
  - ollama
  - local
  - privacy-preserving

framework: langgraph

model:
  primary: ollama/llama3.3

deploy:
  cloud: local
