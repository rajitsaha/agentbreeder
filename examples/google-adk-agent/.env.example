# Google Cloud project for ADK session management and API access
GOOGLE_CLOUD_PROJECT=your-project-id

# Path to your service account key file for Application Default Credentials (ADC).
# When running on GCP (Cloud Run, GKE), omit this and use Workload Identity instead.
GOOGLE_APPLICATION_CREDENTIALS=/path/to/service-account.json
