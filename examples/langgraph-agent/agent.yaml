name: hello-world-agent
version: 0.1.0
description: "A simple hello world LangGraph agent for testing AgentBreeder"
team: examples
owner: dev@agentbreeder.com
tags: [example, langgraph, hello-world]

framework: langgraph

model:
  primary: gpt-4o-mini

deploy:
  cloud: local
